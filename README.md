hi chat. so this is just me screwing around with training next token prediction

try `pip install -r requirements.txt` but likely won't work 100% and ull need to install whatever it complains about
prob need to install flashattention lightning tqdm stuff like that

anyways after thats all done just `mkdir -p models` and `python3 v7_lightning_script.py`
